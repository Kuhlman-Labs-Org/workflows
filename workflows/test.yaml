name: Test Action To Sync

on:
  push:
    branches:
      - '**'

jobs:
  echo-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Echo Test Message
      run: echo "This is a test!!!!!!!!!"
